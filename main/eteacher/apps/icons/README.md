1.直接运行脚本，在apps,bottom,top文件夹内所有图片文件转换成.bin文件；
2.不同类型的图片可以在代码中修改宽和高参数；
3.图片文件.bin中表头有改图片宽和高信息；
4.支持透明背景图片和非透明背景图片的转换；
5.GXEPD2要求行必须是8 bit 的整数倍，否则会出线图像显示错位问题，因此上下栏图标设置成16*16，app为80*80，如果不是8的整数倍需要使用补齐逻辑
6.修改图片资源的时，需要重新构建项目，否则新的图片不会重新加载到asset