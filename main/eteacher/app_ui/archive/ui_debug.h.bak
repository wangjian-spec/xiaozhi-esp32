#pragma once

// Backup of ui_debug.h — removed from active build

#include <cstdint>

namespace app_ui {

class UIDebug {
public:
    void BeginFrame(uint32_t timestamp_ms);
    void EndFrame();

    void SetEnabled(bool enabled);
    bool Enabled() const;

private:
    bool enabled_ = false;
    uint32_t last_frame_ms_ = 0;
};

} // namespace app_ui
